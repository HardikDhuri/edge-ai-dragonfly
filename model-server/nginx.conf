server {
    listen 80;
    server_name localhost;

    location /models/ {
        root /usr/share/nginx/html;
        # Enable byte-range requests — critical for chunked downloads!
        add_header Accept-Ranges bytes;
        add_header Cache-Control "public, max-age=3600";
    }
}